# AI Dev Crew — Environment Variables
# Copy to .env and fill in your values.
# These are loaded automatically by the config loader.

# Required: Anthropic API key for LLM calls
ANTHROPIC_API_KEY=sk-ant-...

# Required: Bearer token for Gateway API authentication
CREW_TOKEN=change-me-to-a-secure-random-string
